# Examples

This folder contains curated artifacts.
Most files are generated from `run_pipeline.py` and are not used at runtime.
`examples/dev_smoke/config.json` is a hand-written smoke config for contract checks.
